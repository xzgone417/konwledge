v-bind：和普通属性的差别。不用：绑定属性那就是写死了。js里面不用dom操作也拿不到也修改不了属性

scoped
样式穿透没生效，可以尝试继续往上取，/deep/

取得接口中的数据时，通过if或者三元表达式判断，获取接口内具体哪个数据，《判断数据是必备的》
将接口内通过判断筛选的值赋给
data(  ){
return    {

},
};
内的值。这样做就可以将数据的身份转化，运用类似于本地数据的传参方法

letter-spacing   字符间的间隔

222
全局组件：
在mian.js声明的组件文件名：大驼峰：页面上组件用大驼峰小驼峰命名都可以

在mian.js声明的组件文件名：小驼峰：
大小写不敏感，大写全部识别为小写，但小写无法识别为大写
333
使用组件时首字母大小写均可
组件文件名：大驼峰：页面上组件用大驼峰小驼峰命名都可以

组件文件名：小驼峰：
大小写不敏感，大写全部识别为小写，但小写无法识别为大写
444
url地址栏里/:id 的：有什么用  ==vue动态路由参数

类似于scorll view的标签

小程序传参用？号，？id=1，   vue动态路由参数