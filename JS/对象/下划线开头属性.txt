class Person {
        //凡是看到下划线开头的属性 都可以认为私有属性
        //私有属性只能够在当前类内部使用
        _name = "";
        _age = 0;


        //为了让私有属性能够被外部访问，我们可以set和get
        //set和get提供了一种间接访问对象私有属性的方式
        //当给对象设置值的时候，会访问对应的set；当获取对象属性值的时候，会访问对应的get